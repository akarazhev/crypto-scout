# RabbitMQ 4.1.3 Configuration for Crypto-Scout
# Core Settings
default_user = admin
default_pass = admin
default_user_tags.administrator = true

# Networking
listeners.tcp.default = 5672
management.tcp.port = 15672
stream.listeners.tcp.1 = 5552

# Memory and Disk Settings
vm_memory_high_watermark.relative = 0.6
disk_free_limit.absolute = 2GB

# Clustering
cluster_formation.peer_discovery_backend = classic_config
cluster_partition_handling = autoheal

# Security
loopback_users = none
log.file.level = info

# Connection and Channel Settings
channel_max = 2047
heartbeat = 60

# Load Definitions
management.load_definitions = /etc/rabbitmq/definitions.json

# Note: Advanced stream and queue settings are configured via policies in definitions.json
# RabbitMQ 4.x doesn't support these direct configuration parameters in rabbitmq.conf
